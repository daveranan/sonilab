# Audio Test Fixtures

Run this from the repo root:

```powershell
npm run fixtures:audio
```

The script always generates a small WAV fixture. If FFmpeg is available, it also generates MP3, OGG Vorbis, and FLAC versions plus `manifest.json`.
